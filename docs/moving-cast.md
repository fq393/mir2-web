# 走跑中施法（2026-09-11）

用户验收要求：走路或跑步时按技能，应停止移动再施放。

根因：MirWorld.cast 在 step 存在时直接返回，技能键被吞掉；原成功施法也没有清除方向键。

本次火球术和治愈术：按键时记录有效目标，清除后续路径、鼠标保持、方向键、跑步意图和自动拾取；保留已经发送的一步及其服务器确认，完成后只发送一次技能。等待期间目标移动读取最新坐标，死亡/消失取消。失焦、断线、切图、Esc或新地面导航取消等待。3秒超时只用于丢弃旧输入，不改变游戏技能冷却。无效目标/未学习技能不会创建等待。

参考：固定 Crystal 0e315fe 的 Client/MirScenes/GameScene.cs UseSpell 保存 NextMagic / NextMagicObject（1108–1110），说明输入意图与后续执行分离；此源码包含后续版本功能，不当作官方1.76逐帧证据。

验证：142项Node测试全通过；新增走路/跑步确认前不施法、确认后一次施法、按键目标保持、移动输入清空、取消/死亡/超时测试。TypeScript检查和Web构建通过。

未完成：本轮未做真实浏览器长按左右键/方向键与F1交替录像，延迟网络下的手感仍待验收；其他未接入技能没有因此完成。SYS-03-04保留“部分”。

## 真实浏览器补验（2026-09-11）

原始采样JSON保存在本地docs/qa/moving-cast，按源码导出策略不上传；本文保留结果摘要。

隔离 PriorityQA（17180/17610），原测试账号，主服务17080及主角色未修改。使用真实鼠标/键盘输入；仅只读提取运行状态。

- 方向S移动一步中按F2：记录到step seq2及待施法61，方向键清空；落在290:614后停止，MP26→23，收到施放治愈术与恢复6生命。
- 方向A移动中悬停稻草人按F1：step seq4保留，待施法目标19；落在289:614后停止，目标保持19，服务器返回4点伤害，无连接错误。首次对鹿的坐标选点未命中，未计为通过。
- 右键按住移动中F2：后续4格路径立即清空，当前步保留，治疗后不继续移动，见qa/moving-cast/right-hold.json。
- 两格跑步中F2：step为285:615→287:615且running=true；结束后step=null/path=[]/held=null，治疗成功，见qa/moving-cast/run.json。
- 包裹/人物按钮各开关一次，四次操作均保持287:615、无step/path/held，没有点击穿透，见qa/moving-cast/ui-clicks.json。此证据不覆盖所有窗口拖动和叠窗。

本次只补上述用例，不将全键盘/全部技能/延迟网络组合全部标为完成。原先“本轮未做浏览器”说明对应初次代码提交，现由这些限定范围的补验更新。
